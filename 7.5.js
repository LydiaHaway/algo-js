const readlineSync = require("readline-sync");

// Basic method

function sortArray(array) {
  return array.sort();
}

array = [25, 10, 60, 30, 54, 98, 51, 50];

console.log(sortArray(array));

// without using the sort function?

arr = [6, 3, 15, 35, 21, 90, 85];
